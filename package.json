{
    "name": "postman-api-tests",
    "version": "1.0.0",
    "description": "API test project JSONPlaceholder Postman + Newman + GitHub Actions",
    "scripts": {
        "build": "tsc",
        "test": "ts-node src/runTests.ts"
    },
    "devDependencies": {
        "@types/node": "^25.2.2"
    },
    "dependencies": {
        "newman": "^6.2.2"
    }
}
